{
  "_comment": "Declarations in this repo that shadow a webindex export, with why they have not been adopted. A ratchet: entries may leave, never arrive. Anything not listed fails verify-engine-usage, so the next fork is an argued decision rather than a quiet copy. An entry that no longer matches anything also fails, so the list cannot rot.",
  "forks": {
    "src/research/cache.ts:cacheDir": "adopting is a STORAGE decision now, not a capability one. The engine grew ETag revalidation, stats and eviction in v1.13, so the old reason (“the engine’s cache has to grow first”) no longer holds — but this one still splits an entry into metadata and body files, so a multi-megabyte page is never JSON-escaped through a parse/stringify round-trip. The engine stores one JSON blob.",
    "src/research/fetch.ts:HttpResult": "fetch stack genuinely diverges from the engine’s — conditional GET / streaming caps / consent stripping. Reconciling it is a behaviour decision, not a dedup.",
    "src/research/fetch.ts:httpGet": "fetch stack genuinely diverges from the engine’s — conditional GET / streaming caps / consent stripping. Reconciling it is a behaviour decision, not a dedup.",
    "src/research/fetch.ts:httpJson": "fetch stack genuinely diverges from the engine’s — conditional GET / streaming caps / consent stripping. Reconciling it is a behaviour decision, not a dedup.",
    "src/research/fetch.ts:htmlToText": "fetch stack genuinely diverges from the engine’s — conditional GET / streaming caps / consent stripping. Reconciling it is a behaviour decision, not a dedup.",
    "src/research/fetch.ts:fetchAndExtract": "fetch stack genuinely diverges from the engine’s — conditional GET / streaming caps / consent stripping. Reconciling it is a behaviour decision, not a dedup.",
    "src/research/fetch.ts:PDF_URL_RE": "private copy inside the forked fetch stack, surfaced once verify-engine-usage stopped requiring a leading `export` to see a shadow. Goes with fetch.ts, not before it.",
    "src/research/firecrawl.ts:firecrawlBase": "the engine's client is a superset, but this skill's fetch.ts consumes it and fetch.ts is not adopted yet — swap both together or neither.",
    "src/research/firecrawl.ts:probeFirecrawl": "the engine's client is a superset, but this skill's fetch.ts consumes it and fetch.ts is not adopted yet — swap both together or neither.",
    "src/research/firecrawl.ts:mapScrapeResponse": "the engine's client is a superset, but this skill's fetch.ts consumes it and fetch.ts is not adopted yet — swap both together or neither.",
    "src/research/firecrawl.ts:scrapeViaFirecrawl": "the engine's client is a superset, but this skill's fetch.ts consumes it and fetch.ts is not adopted yet — swap both together or neither.",
    "src/research/firecrawl.ts:searchViaFirecrawl": "the engine's client is a superset, but this skill's fetch.ts consumes it and fetch.ts is not adopted yet — swap both together or neither.",
    "src/clone.ts:resolveRepo": "the engine now owns a repo layer, but its RepoRef and this one differ in shape. Swapping it touches every call site and every fixture — a behaviour decision that deserves its own pass, not a rename.",
    "src/clone.ts:ensureClone": "the engine now owns a repo layer, but its RepoRef and this one differ in shape. Swapping it touches every call site and every fixture — a behaviour decision that deserves its own pass, not a rename.",
    "src/types.ts:RepoRef": "the engine now owns a repo layer, but its RepoRef and this one differ in shape. Swapping it touches every call site and every fixture — a behaviour decision that deserves its own pass, not a rename.",
    "src/providers/github.ts:apiBase": "the engine’s forge client is a superset, but this repo’s providers/ carry a keyword-relaxation ladder and a coverage rerank the engine does not model. Swap the whole provider layer or none of it.",
    "src/providers/github.ts:canonicalRepo": "the engine’s forge client is a superset, but this repo’s providers/ carry a keyword-relaxation ladder and a coverage rerank the engine does not model. Swap the whole provider layer or none of it.",
    "src/research/cache.ts:CacheEntry": "adopting is a STORAGE decision now, not a capability one. The engine grew ETag revalidation, stats and eviction in v1.13, so the old reason (“the engine’s cache has to grow first”) no longer holds — but this one still splits an entry into metadata and body files, so a multi-megabyte page is never JSON-escaped through a parse/stringify round-trip. The engine stores one JSON blob.",
    "src/research/cache.ts:revalidationHeaders": "adopting is a STORAGE decision now, not a capability one. The engine grew ETag revalidation, stats and eviction in v1.13, so the old reason (“the engine’s cache has to grow first”) no longer holds — but this one still splits an entry into metadata and body files, so a multi-megabyte page is never JSON-escaped through a parse/stringify round-trip. The engine stores one JSON blob.",
    "src/research/cache.ts:CacheStats": "adopting is a STORAGE decision now, not a capability one. The engine grew ETag revalidation, stats and eviction in v1.13, so the old reason (“the engine’s cache has to grow first”) no longer holds — but this one still splits an entry into metadata and body files, so a multi-megabyte page is never JSON-escaped through a parse/stringify round-trip. The engine stores one JSON blob.",
    "src/research/fetch.ts:stripConsentBoilerplate": "fetch stack genuinely diverges from the engine’s — conditional GET / streaming caps / consent stripping. Reconciling it is a behaviour decision, not a dedup.",
    "src/research/fetch.ts:metaDescriptionOf": "fetch stack genuinely diverges from the engine’s — conditional GET / streaming caps / consent stripping. Reconciling it is a behaviour decision, not a dedup.",
    "src/util.ts:ShResult": "the engine’s ShResult reports status as a number and missing as optional; this one keeps status nullable and missing always present, and callers read both. Reconciling is a typing decision, not a dedup.",
    "src/util.ts:sh": "the engine’s ShResult reports status as a number and missing as optional; this one keeps status nullable and missing always present, and callers read both. Reconciling is a typing decision, not a dedup.",
    "src/util.ts:shAsync": "the engine’s ShResult reports status as a number and missing as optional; this one keeps status nullable and missing always present, and callers read both. Reconciling is a typing decision, not a dedup.",
    "src/util.ts:have": "the engine’s ShResult reports status as a number and missing as optional; this one keeps status nullable and missing always present, and callers read both. Reconciling is a typing decision, not a dedup."
  }
}
