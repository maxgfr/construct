{
  "name": "construct",
  "version": "3.14.0",
  "description": "Turn a product idea into a grounded, buildable SRD suite \u2014 requirements grounded in real market, OSS prior-art, and tech-feasibility research, not the model's memory. A skills.sh agent skill.",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "bin": {
    "construct": "scripts/construct.mjs"
  },
  "files": [
    "assets",
    "scripts",
    "skills",
    "src"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsup && node scripts/copy-bundle.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "biome ci .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "verify:bundle": "node scripts/verify-skill-bundle.mjs",
    "check:build": "node scripts/sync-engine.mjs --check && tsup && node scripts/copy-bundle.mjs && git diff --exit-code -- scripts/construct.mjs skills/construct/scripts/construct.mjs && node scripts/verify-skill-bundle.mjs && git diff --exit-code -- assets tests/__snapshots__ && node scripts/verify-engine-usage.mjs",
    "demo": "rm -rf /tmp/construct-demo && mkdir -p /tmp/construct-demo/evidence && cp tests/fixtures/sample-brief.json /tmp/construct-demo/brief.json && cp tests/fixtures/sample-evidence.json /tmp/construct-demo/evidence/evidence.json && node scripts/construct.mjs render --out /tmp/construct-demo --level light --merge && node scripts/construct.mjs check --out /tmp/construct-demo",
    "release": "semantic-release",
    "example": "node scripts/construct.mjs render --out assets/example-srd --from-srd && node scripts/construct.mjs check --out assets/example-srd --min-grounding 66",
    "verify:engine": "node scripts/verify-engine-usage.mjs"
  },
  "keywords": [
    "skills",
    "agent-skill",
    "skills.sh",
    "srd",
    "prd",
    "requirements",
    "product",
    "grounded-research",
    "ai-agent"
  ],
  "license": "MIT",
  "author": "maxgfr",
  "repository": {
    "type": "git",
    "url": "https://github.com/maxgfr/construct"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@types/node": "^20.14.0",
    "@vitest/coverage-v8": "^4.1.8",
    "semantic-release": "^24.2.0",
    "tsup": "^8.3.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.0",
    "yaml": "^2.9.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
